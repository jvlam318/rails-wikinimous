<%= form_for(@article, :html => {:class => "form-group"}) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title, class: "form-control" %>

  <%= t.label :content %>
  <%= t.text_field :content, class: "form-control" %>

  <%= t.submit%>
<% end %>
